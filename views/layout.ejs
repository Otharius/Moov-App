<html lang="fr">
<title><%= title %></title>
<head>
  <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
  <script src="https://code.jquery.com/jquery-1.10.2.js"></script>  
  <script src="https://code.jquery.com/ui/1.10.4/jquery-ui.js"></script>  
  <link href="/css/computer.css" media="(min-width: 480px)" rel="stylesheet" type="text/css">
  <link rel="stylesheet" href="/css/mobil.css" media="(max-width: 480px)" type="text/css">
  <script src="https://kit.fontawesome.com/2af4903d5e.js" crossorigin="anonymous"></script>

    <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.2.1/jquery.min.js"></script>
 
    <script src="https://cdn.jsdelivr.net/npm/select2@4.0.13/dist/js/select2.min.js"></script>



  <meta name="viewport" content="width=device-width"/>
  <meta http-equiv="X-UA-Compatible" content="ie=edge" />
</head>
 
<body onload="test()">
  <% if(style){ %>
    <span class="navbar">
      <a href="/workout/training" class="nav_second">
        <i class="fa-solid fa-football fa-2xl"></i>
      </a>
  
      <a href="/meal_sleep/sleep" class="nav_second">
        <i class="fa-solid fa-moon fa-2xl"></i>
      </a>
  
      <a href="/home/home" class="nav_home">
        <i class="fa-solid fa-house-chimney fa-2xl"></i>
      </a>
  
      <a href="/meal_sleep/meal" class="nav_second">
        <i class="fa-solid fa-apple-whole fa-2xl"></i>
      </a>
  
      <a href="/profiles/profiles" class="nav_second">
        <i class="fa-solid fa-user fa-2xl"></i>
      </a>
    </span>
<% }%>

  <%- body %>
</body>
<script>

$(document).ready(function () {
//change selectboxes to selectize mode to be searchable
   $(".exerciceSelect").select2();
});

function test () {
  $('.template').attr("style", "display:none");
  $('.field').attr("name", '');
  const id = '#' + $('#selectExerciceJob').val();
  $(id).attr("style", "display:block");
  $(id + 'Serie').attr("name", "serie");
  $(id + 'Repetition').attr("name", "repetition");
  $(id + 'Exercice').attr("name", "exercice");
  $(id + 'Pause').attr("name", "pause");
  $(id + 'Charge').attr("name", "charge");
  $(id + 'Time').attr("name", "time");
  $(id + 'Distance').attr("name", "distance");
  $(id + 'Duration').attr("name", "duration");
  $(id + 'Start').attr("name", "start");
  $(id + 'Arrival').attr("name", "arrival");

}

$(function() {  
    $("#table_intensity_contenta").draggable();  
  });


  
const wrapper = document.querySelector(".move_div"),
header = wrapper.querySelector("header");

function onDrag({movementX, movementY}){
    let getStyle = window.getComputedStyle(wrapper);
    let leftVal = parseInt(getStyle.left);
    let topVal = parseInt(getStyle.top);
    wrapper.style.left = `${leftVal + movementX}px`;
    wrapper.style.top = `${topVal + movementY}px`;
}
  header.addEventListener("mousedown", () => {
    header.classList.add("active");
    header.addEventListener("mousemove", onDrag);
});
  document.addEventListener("mouseup", () => {
    header.classList.remove("active");
    header.removeEventListener("mousemove", onDrag);
});


function closeCreateDiv () {
  const div = document.getElementById('wrapper');
  const btn = document.getElementById('btnDraggable');
  
  if (div.style.display == 'block') {
    div.style.display = 'none';
    choix()
  } else {
    div.style.display = 'block';
    div.style.position = 'absolute';
    choix()
  };
};

function intensity_div () {
  const div = document.getElementById('table_intensity_content');
  const btn = document.getElementById('btnTable');
  
  if (div.style.display == 'block') {
    div.style.display = 'none';
  } else {
    div.style.display = 'block';
    div.style.position = 'absolute';
  };
};






</script>
</html>
